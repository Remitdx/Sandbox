<%= simple_form_for(player) do |f| %>
  <div class="d-flex justify-content-center">
    <%= f.input :pseudo, label: false, placeholder: 'Pseudo' %>
    <%= f.hidden_field :risk_id, :value => risk.id %>
    <%= f.submit 'Add', class:'btn btn-primary' %>
  </div>
<% end %>
